"""live.live_signals — live breach check of the analysis_signals
threshold set for one code (the entry point __main__.py; config/ —
tables / runtime sources / breach semantics; analysis/ — the ONE
generic breach check evaluator.py over analysis/fetch/ — value
resolution + source, bar and active-set fetchers).
"""
