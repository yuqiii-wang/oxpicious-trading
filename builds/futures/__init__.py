"""builds.futures — Build CFFEX futures baseline data and insert to database.

Submodules:
  config  — Product code mappings, column definitions
  paths   — Source directory paths for CFFEX archive CSV files
  loader  — CSV reading/parsing logic
  __main__ — Entry point (missing-dates-only → DATABASE)
"""