"""downloads.futures.cffex.trend — Download CFFEX daily trend data.

Submodules:
  config     — Configuration for URLs, paths, date logic
  paths      — Directory path helpers for trend CSV files
  downloader — Playwright-based browser automation for downloading
  __main__   — Entry point with full workflow (check DB → backfill → download)
"""