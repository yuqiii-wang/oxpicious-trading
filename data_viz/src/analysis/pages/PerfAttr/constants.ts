/**
 * Shared constants for the Performance Attribution analysis page sub-modules.
 */

/**
 * Page size — number of PerfAttrPanel cards shown per page. Kept small
 * because each panel renders up to three charts plus an expandable
 * time-series section, so larger pages get unwieldy.
 */
export const PAGE_SIZE = 2;
