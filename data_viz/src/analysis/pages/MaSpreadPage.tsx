/**
 * Thin re-export so existing imports of `@/analysis/pages/MaSpreadPage`
 * continue to resolve. The actual implementation lives in the
 * `./MaSpread/` subdirectory (split into smaller modules).
 */
export { default } from "./MaSpread";
