/**
 * Shared constants for the Recurring Cycles analysis page sub-modules.
 */

/**
 * Page size — number of RecurringCyclesPanel cards shown per page. Kept to 1
 * because each panel renders a full-height index plot + spectrum charts;
 * multiple panels per page would be visually overwhelming.
 */
export const PAGE_SIZE = 1;
