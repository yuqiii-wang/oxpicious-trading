/**
 * Thin re-export so existing imports of `@/analysis/pages/IndustrySentimentsPage`
 * continue to resolve. The actual implementation lives in the
 * `./IndustrySentiments/` subdirectory (split into smaller modules).
 */
export { default } from "./IndustrySentiments";
