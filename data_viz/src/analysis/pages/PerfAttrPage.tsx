/**
 * Thin re-export so existing imports of `@/analysis/pages/PerfAttrPage`
 * continue to resolve. The actual implementation lives in the
 * `./PerfAttr/` subdirectory (split into smaller modules).
 */
export { default } from "./PerfAttr";
