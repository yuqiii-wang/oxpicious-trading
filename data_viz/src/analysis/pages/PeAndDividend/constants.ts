/**
 * Shared constants for the PE & Dividend Yield analysis page sub-modules.
 */

/**
 * Page size — number of PeAndDividendPanel cards shown per page. Kept to 1
 * because each panel renders a full-height valuation chart plus a monthly
 * stats table beneath it; multiple panels per page would be visually
 * overwhelming.
 */
export const PAGE_SIZE = 1;
