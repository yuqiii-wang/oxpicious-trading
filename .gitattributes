# Shell scripts must keep LF endings: they are executed by bash inside
# Linux containers and WSL, where CRLF breaks parsing (e.g. the db-replica
# entrypoint bind-mounted into the postgres image).
*.sh text eol=lf
