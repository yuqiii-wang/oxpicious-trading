"""Recurring Cycles analysis (ETF + Index + Stock).

Populates analysis.recurring_cycles — per-(sec_type, code, last_date,
range_days) recurring rise/drop periodicity of close prices.

Run via ``python -m analyze.recurring_cycles``.
"""
